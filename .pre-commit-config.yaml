repos:
  - repo: https://github.com/astral-sh/ruff-pre-commit
    rev: v0.15.12
    hooks:
      - id: ruff-check

  - repo: local
    hooks:
      - id: export-openapi
        name: Export OpenAPI schema
        entry: bash scripts/export_openapi.sh
        language: system
        pass_filenames: false
        files: ^routers/|^responses/
        stages: [pre-commit]

      - id: check-arch
        name: architecture lint
        entry: venv/bin/python scripts/check_arch.py
        language: system
        pass_filenames: false
        stages: [pre-push]

      - id: pyright
        name: pyright
        entry: venv/bin/pyright
        language: system
        pass_filenames: false
        stages: [pre-push]

      - id: pytest-unit
        name: pytest (unit)
        entry: venv/bin/pytest
        args: [-m, unit, --tb=short, -q]
        language: system
        pass_filenames: false
        stages: [pre-push]
